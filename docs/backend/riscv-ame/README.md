
## Build Instructions for RISC-V AME Backend

To build the RISC-V AME backend, run the following script:

```bash
./docs/backend/riscv-ame/build-ame.sh
```

This script will compile the necessary files for the RISC-V AME backend.

Make sure you have all required dependencies installed before running the script.